const Container = ({ children }) => {
  return <View style={styles.container}> {children} </View>
}
 - Todo conteúdo que passar dentro de Container, estará envolto a esse tag View e com a propriedade definida em styles


const Input = (props) => {
  return (
    <TextInput
      style={styles.input}
      {...props}
    />
  );
}

{... props}
- Define que vai receber todas as propriedades que tem de estilos